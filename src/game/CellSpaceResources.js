require(["Utils"], function(utils) {
/*
	var loader = new utils.ResLoader();
	var enemyData = loader.load("enemy", "rules/moore4_wave");
	loader.load("ship", "dsafasdGF");
	loader.start(function (data) {
		alert(data.enemy);
		alert(enemyData.value);
	});

	return {
    enemyRuleRaw: "black",
    size: "unisize"
	}
*/
});
